export type ReviewCard = {
    id: string;           // Identificador único de la reseña
    name: string;         // Nombre del usuario que hizo la reseña
    rating: number;       // Puntuación del 1 al 5
    description: string;  // Contenido de la reseña
};